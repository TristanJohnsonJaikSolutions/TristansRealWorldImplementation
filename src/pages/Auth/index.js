import { Auth } from "./Auth";

// This file re-exports the named Login component so you can import the component like this:
//     import Auth from "pages/Auth";
// Without this the imports would need to look like this:
//     import { Auth } from "pages/Auth/Auth";
export default Auth;
